# Quando a API serve o build do frontend (mesma origem/porta), deixamos
# VITE_API_URL vazio para que as chamadas fiquem relativas (ex: fetch('/customers')
# em vez de fetch('http://localhost:8080/customers')).
VITE_API_URL=
